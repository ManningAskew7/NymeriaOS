<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Enforce TLS for every backend by default. The app attaches an
      `Authorization: Bearer <token>` header to every request, so cleartext
      HTTP would expose the token (and all chat traffic) to on-path attackers
      on open Wi-Fi or a hostile network. Cleartext is permitted ONLY for local
      development loopback and emulator host aliases; a real (non-loopback)
      backend must be reached over https://.
    -->
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
    </domain-config>
</network-security-config>
