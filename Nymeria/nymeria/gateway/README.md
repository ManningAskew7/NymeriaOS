# gateway/

Gateway server for foreground `service` mode (`python run.py service`).

## Start here

`server.py`  -  `GatewayServer` class.

## Contents

- `server.py`  -  gateway server implementation
- `transports/`  -  transport layer (currently `rest.py` for REST transport)
