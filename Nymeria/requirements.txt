# Nymeria - Personal AI Assistant Framework
# Core dependencies

# LangGraph/LangChain — pinned to compatible ranges matching VPS (1.1.x) and Docker (1.2.x)
langchain>=1.2.17,<2.0.0
langchain-core>=1.3.2,<2.0.0
langchain-openai>=1.2.1,<2.0.0
langchain-anthropic>=1.4.2,<2.0.0
langgraph>=1.1.10,<2.0.0
langgraph-prebuilt>=1.0.13,<2.0.0
langgraph-checkpoint>=4.0.3,<5.0.0
langgraph-sdk>=0.3.13,<1.0.0
langchain-community>=0.4.1,<1.0.0  # optional: wikipedia, wolfram alpha, searxng tools

# SQLite persistence and local vector search (default - zero setup)
-r requirements-sqlite.txt

# API Framework
fastapi>=0.136.1,<1.0.0
starlette>=1.0.0,<2.0.0
uvicorn[standard]>=0.32.0,<1.0.0
sse-starlette>=2.1.0

# Configuration
pydantic>=2.9.2,<3.0.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# HTTP client (for web tools)
httpx>=0.27.1,<1.0.0
requests>=2.32.0,<3.0.0  # agent skills marketplace (GitHub API)
urllib3>=2.2.2,<3.0.0
wikipedia>=1.4.0  # wikipedia_search optional tool
wolframalpha>=5.0.0  # wolfram_alpha_query optional tool

# Agent Skills — SKILL.md frontmatter parser
PyYAML>=6.0.2,<7.0.0

# Utilities
python-multipart>=0.0.12
rich>=13.9.0
markdown-it-py>=3.0.0
prompt_toolkit>=3.0.0
Jinja2>=3.1.6,<4.0.0
Pillow>=10.4.0,<12.0.0

# MCP Server support (for agent-to-agent communication)
mcp>=1.2.0

# Discord bot (two-way communication)
discord.py>=2.3.0

# Twitch bot (two-way communication)
twitchio>=3.2.1

# Telegram bot (two-way communication)
python-telegram-bot[rate-limiter]>=22.0

# Slack bot (two-way communication via Socket Mode)
slack-bolt>=1.22.0
slack-sdk>=3.35.0

# Symmetric encryption for at-rest secrets (BYO Telegram bot tokens, etc).
# Already a transitive dep via langchain/google-auth, but pinned explicitly
# so the secrets layer never silently breaks if upstream drops it.
cryptography>=42.0.4,<47.0.0
PyJWT>=2.8.0

# Optional: PostgreSQL support for local development. Install in addition to
# this file when DATABASE_BACKEND=postgres:
# pip install -r requirements-postgres.txt

# Google Calendar (native Python — optional tools)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0

# Document extraction (email attachments) + Gemini TTS
google-genai>=1.0.0
pydub>=0.25.1
openpyxl>=3.1.0

# RSS/Atom feed parsing (trigger source)
feedparser>=6.0.0
