# Nymeria - Personal AI Assistant Framework
# Core dependencies

# LangGraph/LangChain
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langgraph>=0.2.0
langgraph-checkpoint>=0.2.0

# SQLite persistence (default - zero setup)
langgraph-checkpoint-sqlite>=1.0.0
aiosqlite>=0.20.0

# Vector search for RAG
sqlite-vec>=0.1.0

# API Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0

# Configuration
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# HTTP client (for web tools)
httpx>=0.27.0

# Utilities
python-multipart>=0.0.12
rich>=13.9.0
prompt_toolkit>=3.0.0

# MCP Server support (for agent-to-agent communication)
mcp>=1.2.0

# Discord bot (two-way communication)
discord.py>=2.3.0

# Twitch bot (two-way communication)
twitchio>=3.2.1

# Telegram bot (two-way communication)
python-telegram-bot[rate-limiter]>=22.0

# Optional: PostgreSQL support (for multi-user deployments)
# Uncomment if you want to use PostgreSQL instead of SQLite:
# langgraph-checkpoint-postgres>=0.2.0
# psycopg[binary]>=3.2.0

# Google Calendar (native Python — optional tools)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0

# Document extraction (email attachments) + Gemini TTS
google-genai>=1.0.0
pydub>=0.25.1
openpyxl>=3.1.0

# Windows Service Support
pywin32>=306; sys_platform == 'win32'
