# Nymeria - Personal AI Assistant Framework
# Core dependencies

# LangGraph/LangChain tool-call adapter stack.
# Exact-pinned after the 2026-05-18 tool_enable regression, where the
# model/provider path emitted JSON-encoded list args for an array-typed tool
# field. Update deliberately with the tool-call smoke tests documented in
# docs/production/10-configuration-and-dependencies.md.
langchain==1.3.1
langchain-core==1.4.0
langchain-openai==1.2.1
langchain-anthropic==1.4.3
# Tier 1 partner packages with native reasoning round-trip. Adopted because
# their dedicated paths fix correctness gaps in the OpenAI-compat shim
# (e.g., Gemini 3+ thought signatures, Bedrock Converse thinking).
langchain-google-genai==4.1.2
langchain-aws==1.4.6
langchain-ollama==1.1.0
langchain-classic==1.0.7
langchain-community==0.4.1  # optional: wikipedia, wolfram alpha tools
langchain-protocol==0.0.15
langchain-text-splitters==1.1.2
langgraph==1.2.0
langgraph-prebuilt==1.1.0
langgraph-checkpoint==4.1.0
langgraph-sdk==0.3.14
anthropic==0.102.0
openai==2.37.0

# SQLite persistence and local vector search (default - zero setup)
-r requirements-sqlite.txt

# API Framework
fastapi>=0.136.1,<1.0.0
starlette>=1.0.0,<2.0.0
uvicorn[standard]>=0.32.0,<1.0.0
sse-starlette>=2.1.0

# Configuration
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv>=1.0.0

# HTTP client (for web tools)
httpx>=0.27.1,<1.0.0
requests>=2.32.0,<3.0.0  # agent skills marketplace (GitHub API)
urllib3>=2.2.2,<3.0.0
python-dateutil>=2.9.0,<3.0.0  # calendar-aware TODO recurrence intervals
wikipedia>=1.4.0  # wikipedia_search optional tool
wolframalpha>=5.0.0  # wolfram_alpha_query optional tool

# Agent Skills — SKILL.md frontmatter parser
PyYAML>=6.0.2,<7.0.0

# Utilities
python-multipart>=0.0.12
rich>=13.9.0
markdown-it-py>=3.0.0
prompt_toolkit>=3.0.0
textual>=0.80.0
Jinja2>=3.1.6,<4.0.0
Pillow>=10.4.0,<13.0.0
qrcode>=8.0,<9.0  # terminal QR codes (setup wizard: tailscale login, pairing handoff)

# MCP Server support (for agent-to-agent communication)
mcp>=1.2.0

# Chat-platform bot SDKs are NOT core dependencies. They live in
# requirements-bots.txt (and the nymeria[<platform>] extras) so a default/slim
# install stays lean. The full Docker image pulls them via requirements-docker.txt.

# Symmetric encryption for at-rest secrets (BYO Telegram bot tokens, etc).
# Already a transitive dep via langchain/google-auth, but pinned explicitly
# so the secrets layer never silently breaks if upstream drops it.
cryptography>=42.0.4,<47.0.0
PyJWT>=2.8.0

# Optional: PostgreSQL support for local development. Install in addition to
# this file when DATABASE_BACKEND=postgres:
# pip install -r requirements-postgres.txt

# Google Calendar (native Python — optional tools)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0

# Document extraction (email attachments, user file attachments) + Gemini TTS
google-genai>=1.0.0
pydub>=0.25.1
# Edge TTS (free, keyless hosted voice; tiny pure-python client)
edge-tts>=7.0.0
openpyxl>=3.1.0
pypdf>=4.0.0
python-docx>=1.1.0

# RSS/Atom feed parsing (trigger source)
feedparser>=6.0.0

# Web page fetch + content extraction (fetch_url_nymeria). Trafilatura is the
# primary article extractor (emits markdown); readability-lxml + markdownify are
# the fallback when Trafilatura returns thin content.
trafilatura>=1.12.0
readability-lxml>=0.8.1
lxml>=5.0.0
markdownify>=0.13.0
