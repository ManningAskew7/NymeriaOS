# Docker-only dependencies (supplements requirements.txt — include specialized
# requirement files instead of duplicating package entries).

# Redis support for event bus
redis>=5.0.0

# PostgreSQL support for production deployments
-r requirements-postgres.txt

# Firebase Cloud Messaging (push notifications to watch/mobile devices)
firebase-admin>=6.0.0
