# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
.venv
env/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.tox/
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.mypy_cache/

# Data (mounted as volume in production)
data/
*.db
*.db-journal

# Local operational state and scratch, never image content. NB dockerignore
# patterns are NOT gitignore: *.db above matches only at the context root,
# so nested artifacts (runs/blended_eval/*.db) must be excluded by directory.
runs/
local_models/
tmp/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Git
.git/
.gitignore

# Docker
Dockerfile
docker-compose*.yml
.dockerignore

# Documentation (not needed in image)
docs/
*.md
!requirements*.txt

# Development files
.env.example
.env.docker
.env.docker.*
.env.docker.example
# A leftover local-install config would bake into the image (secrets included)
# and load inside the container with override=True.
config.env
tests/
