# PostgreSQL checkpoint dependencies for local development.
# Install in addition to requirements.txt when DATABASE_BACKEND=postgres.

langgraph-checkpoint-postgres>=3.1.0,<4.0.0
psycopg[binary]>=3.3.4,<4.0.0
psycopg-pool>=3.3.1,<4.0.0
